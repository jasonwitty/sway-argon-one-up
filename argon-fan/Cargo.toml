[package]
name = "argon-fan"
version = "0.1.0"
edition = "2021"
description = "PWM fan control daemon for the Argon ONE UP CM5 with mode-based curves"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
signal-hook = "0.3"
